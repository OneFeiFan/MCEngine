<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MCEngine" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/雪青</item>
        <item name="colorPrimaryDark">@color/紫罗兰</item>
        <item name="android:statusBarColor">@color/紫罗兰</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.MCEngine" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/雪青</item>
        <item name="colorPrimaryDark">@color/紫罗兰</item>
        <item name="android:statusBarColor">@color/紫罗兰</item>
    </style>
</resources>